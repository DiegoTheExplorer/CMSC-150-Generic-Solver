nvt <- read.csv("csv/nutriVals.csv") #Nutritional Value Table

SimplexDietSolver <- function(poods,constraints){
  
  print(poods)
  print(constraints)
  
  # Start Objective Function Creation
  
  # End Objective Function Creation
}